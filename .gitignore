# Build artifacts
*.o
*.a.tmp

# Pre-built static archives are committed for zero-bootstrap Go module usage.
# Only ignore temp build artifacts, not the final .a files.

# Design docs (local only)
docs/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
